archetype contract_caller

variable r : nat = 0

entry inspect(addr : address) {
  // FIXME: call entry "%get_n" from addr address contract
  // with a callback which store the result value to r of this contract
}
