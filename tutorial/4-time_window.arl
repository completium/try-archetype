archetype time_window

variable creation : date = now

entry payback_after_period () {
  // FIXME: the contract balance must be positive
  // and the call of the contract must be after the creation date plus 5 minutes and 10 seconds
  // for the effect, the contract balance must be transferred to caller
}
